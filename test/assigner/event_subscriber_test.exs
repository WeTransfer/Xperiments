defmodule Xperiments.Assigner.EventSubscriberTest do
  use Xperiments.AssignCase

  # test "handle random messages and log them" do
  #   Xperiments.Endpoint.broadcast("experiments:all", "random", %{})
  # end
end
